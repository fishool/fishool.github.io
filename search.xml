<?xml version="1.0" encoding="utf-8"?>
<search> 
  
    
    <entry>
      <title><![CDATA[Spring aop 入门(三)]]></title>
      <url>//blog/2018/01/25/Spring-aop-%E5%85%A5%E9%97%A8-%E4%B8%89/</url>
      <content type="html"><![CDATA[<p>第四天<br>1  advisor  增强器<br>   作用:<strong>筛选指定要拦截(要代理)的方法</strong><br>        默认情况下advice会拦截目标对象中的所有方法，advisor增强器可以进一步进行方法的筛选，筛选的方法就是我们的adivce需要拦截的方法。<br>   指定为advisor的接口为:<br>    public interface PointcutAdvisor {<br>        Pointcut getPointcut();<br>        Advice getAdvice();<br>    }<br>    spring中已经给我们提供了实现类RegexpMethodPointcutAdvisor,在xml中直接配使用就可以了</p>
<pre><code>xml配置文件:
&lt;!-- 配置切面类 --&gt;
&lt;bean name=&quot;logger&quot; class=&quot;com.briup.aop.aspect.MyLogger&quot;&gt;&lt;/bean&gt;

&lt;!-- 配置advice --&gt;
&lt;bean name=&quot;beforeAdvice&quot; 
class=&quot;com.briup.aop.before.BeforeAdvice&quot;&gt;
    &lt;!-- 注入切面类对象 --&gt;
    &lt;property name=&quot;logger&quot; ref=&quot;logger&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;
</code></pre><p><img src="https://gss2.bdstatic.com/-fo3dSag_xI4khGkpoWK1HF6hhy/baike/c0%3Dbaike80%2C5%2C5%2C80%2C26/sign=892cd34586d6277ffd1f3a6a49517455/b90e7bec54e736d12103cbf69a504fc2d562693f.jpg" alt="面向切面"></p>
<a id="more"></a>
<pre><code>&lt;!-- 配置advisor 增强器--&gt;
&lt;!-- 作用:筛选要拦截(要代理)的方法 --&gt;
&lt;bean name=&quot;advisor&quot; 
class=&quot;org.springframework.aop.support.RegexpMethodPointcutAdvisor&quot;&gt;
    &lt;!-- 注入advice 表示增强器要在哪一个advice起作用--&gt;
    &lt;property name=&quot;advice&quot; ref=&quot;beforeAdvice&quot;&gt;&lt;/property&gt;
    &lt;!-- 注入需要被拦截的目标对象中的方法(连接点) --&gt;
    &lt;property name=&quot;patterns&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;.*bankAction&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;
&lt;/bean&gt;

&lt;bean id=&quot;dao&quot; 
 class=&quot;com.briup.aop.dao.AccountDaoImpl&quot;/&gt;
&lt;!-- 配置目标对象 --&gt;
&lt;bean name=&quot;target&quot; 
class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;

&lt;!-- 配置代理对象 --&gt;
&lt;!-- 这里使用的是spring的一个代理对象工厂类产生的 --&gt;
&lt;bean name=&quot;proxy&quot; 
class=&quot;org.springframework.aop.framework.ProxyFactoryBean&quot;&gt;
    &lt;!-- 注入目标对象 --&gt;
    &lt;property name=&quot;target&quot; ref=&quot;target&quot;&gt;&lt;/property&gt;

    &lt;!-- 注入目标对象所实现的接口 可以有多个接口 --&gt;
    &lt;property name=&quot;proxyInterfaces&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;com.briup.aop.service.IAccountService&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;

    &lt;!-- 注入advice/advisor  可以有多个 --&gt;
    &lt;property name=&quot;interceptorNames&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;advisor&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;
&lt;/bean&gt;
</code></pre><p>2  AutoProxy 自动代理:DefaultAdvisorAutoProxyCreator类的使用<br>   使用原因:在配置文件中我们往往需要给很多个目标对象设置代理对象,那么上面例子的方式就需要每个目标对象的代理对象都需要配置一套类似的标签<br>   自动代理:可以用很少的配置为xml文件中的目标对象自动的生成对应的代理对象   </p>
<p>   xml配置文件:<br>    <!-- 配置切面类 --><br>    <bean name="logger" class="com.briup.aop.aspect.MyLogger"></bean></p>
<pre><code>&lt;!-- 配置advice --&gt;
&lt;bean name=&quot;beforeAdvice&quot; 
class=&quot;com.briup.aop.before.BeforeAdvice&quot;&gt;
    &lt;!-- 注入切面类对象 --&gt;
    &lt;property name=&quot;logger&quot; ref=&quot;logger&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;

&lt;!-- 配置advisor --&gt;
&lt;!-- 作用:筛选要拦截的方法 --&gt;
&lt;bean name=&quot;advisor&quot; class=&quot;org.springframework.aop.support.RegexpMethodPointcutAdvisor&quot;&gt;
    &lt;!-- 注入advice --&gt;
    &lt;property name=&quot;advice&quot; ref=&quot;beforeAdvice&quot;&gt;&lt;/property&gt;
    &lt;!-- 注入需要被拦截的目标对象中的方法 --&gt;
    &lt;property name=&quot;patterns&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;.*bankAction&lt;/value&gt;
            &lt;value&gt;.*deposit&lt;/value&gt;
            &lt;value&gt;.*withdraw&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;
&lt;/bean&gt;

&lt;bean id=&quot;dao&quot; class=&quot;com.briup.aop.dao.AccountDaoImpl&quot;/&gt;

&lt;!-- 配置目标对象 --&gt;
&lt;bean name=&quot;target&quot; 
class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;

&lt;bean name=&quot;target2&quot; 
class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;

&lt;bean name=&quot;target3&quot; 
class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;


&lt;!-- 配置代理对象 --&gt;
&lt;!-- 这里使用自动代理的方式 autoproxy --&gt;
&lt;!-- 注意:这不是一个工厂类,所以不能用过proxy来拿代理对象 --&gt;
&lt;bean name=&quot;proxy&quot; 
class=&quot;org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator&quot;&gt;
&lt;/bean&gt;
&lt;!-- 
    使用自动代理的时候需要注意的方面:
    1.当前的配置里面一定要有一个advisor的配置
    2.不需要向自动代理类中注入任何信息
    3.不管目标对象是否实现了一个或多接口,自动代理的方式
      都能够为它产生代理对象(CGLib的方式).
    4.从spring容器中拿代理对象的时候,需要通过目标对象的
      名字来拿。
5.spring如何确定配置文件中哪个bean是作为目标对象:
  通过advisor中筛选的方法,如果这个bean中含有advisor中所配置的方法,则这个bean将来称为我们的目标对象进行代理
 --&gt;
</code></pre><p>3  AutoProxyByName 通过名字进行自动代理:BeanNameAutoProxyCreator类的使用<br>    使用原因:虽然自动代理可以很方便的给xml文件中的目标对象设置对应的代理对象,但是并不是xml文件中的所有对象都是我们的目标对象,我们更想希望可以进一步筛选出某几个对象为我们的目标对象<br>    名字进行自动代理:解决了上面的问题,给我们提供了筛选目标对象的配置方式   </p>
<pre><code>xml配置文件:
&lt;!-- 配置切面类 --&gt;
&lt;bean name=&quot;logger&quot; class=&quot;com.briup.aop.aspect.MyLogger&quot;&gt;&lt;/bean&gt;

&lt;!-- 配置advice --&gt;
&lt;bean name=&quot;beforeAdvice&quot; 
class=&quot;com.briup.aop.before.BeforeAdvice&quot;&gt;
    &lt;!-- 注入切面类对象 --&gt;
    &lt;property name=&quot;logger&quot; ref=&quot;logger&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;


&lt;!-- 配置advisor --&gt;
&lt;!-- 作用:筛选要拦截的方法 --&gt;
&lt;bean name=&quot;advisor&quot; class=&quot;org.springframework.aop.support.RegexpMethodPointcutAdvisor&quot;&gt;
    &lt;!-- 注入advice --&gt;
    &lt;property name=&quot;advice&quot; ref=&quot;beforeAdvice&quot;&gt;&lt;/property&gt;
    &lt;!-- 注入需要被拦截的目标对象中的方法 --&gt;
    &lt;property name=&quot;patterns&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;.*bankAction&lt;/value&gt;
            &lt;value&gt;.*deposit&lt;/value&gt;
            &lt;value&gt;.*withdraw&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;
&lt;/bean&gt;

&lt;bean id=&quot;dao&quot; 
 class=&quot;com.briup.aop.dao.AccountDaoImpl&quot;/&gt;

&lt;!-- 配置目标对象 --&gt;
&lt;bean name=&quot;target&quot; 
class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;

&lt;bean name=&quot;target2&quot; 
class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;

&lt;bean name=&quot;target3&quot; 
class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;



&lt;!-- 配置代理对象 --&gt;
&lt;!-- 这里使用自动代理的方式 autoproxybyname --&gt;
&lt;bean name=&quot;proxy&quot; class=&quot;org.springframework.aop.framework.autoproxy.BeanNameAutoProxyCreator&quot;&gt;
    &lt;!-- 注入需要被代理的对象名字 --&gt;
       &lt;property name=&quot;beanNames&quot;&gt;
           &lt;list&gt;
               &lt;value&gt;target&lt;/value&gt;
               &lt;value&gt;target2&lt;/value&gt;
               &lt;value&gt;dao&lt;/value&gt;
               &lt;value&gt;service*&lt;/value&gt;
           &lt;/list&gt;
       &lt;/property&gt;

       &lt;!-- 注入advice或者advisor --&gt;
       &lt;property name=&quot;interceptorNames&quot;&gt;
           &lt;list&gt;
               &lt;value&gt;advisor&lt;/value&gt;
           &lt;/list&gt;
       &lt;/property&gt;
&lt;/bean&gt;
&lt;!-- 
    使用自动代理的时候需要注意的方面:
    1.当前的配置里面有没有advisor的配置都没关系
    2.需要向自动代理类中注入被代理目标对象的名字已经advice或者advisor
    3.不管目标对象是否实现了一个或多接口,自动代理的方式
      都能够为它产生代理对象.
    4.从spring容器中拿代理对象的时候,需要通过目标对象的名字来拿。
 --&gt;
</code></pre><p>4  aop:config标签<br>   使用aop的专用标签来完成相关的配置.<br>   其中主要表现是使用AspectJ的expression的操作：<br>   execution(modifiers-pattern ret-type-pattern declaring-type-pattern    name-pattern(param-pattern)    throws-pattern)<strong>除了返回类型模式，方法名字模式和参数模式以外</strong>，所有的部分都是可选的。     返回类型模式决定了方法的返回类型必须依次匹配一个连接点。     你会使用的最频繁的返回类型模式是<em>，它代表了匹配任意的返回类型。    一个全称限定的类型名将只会匹配返回给定类型的方法。名字模式匹配的是方法名。<br>你可以使用 </em> 通配符作为所有或者部分命名模式。 参数模式稍微有点复杂：<br>   ()   匹配了一个不接受任何参数的方法，<br>   而 (..)   匹配了一个接受任意数量参数的方法（零或者更多）。<br>   模式 ( <em> )   匹配了一个接受一个任何类型的参数的方法。<br>   模式 (</em>,String)   匹配了一个接受两个参数的方法，第一个可以是任意类型，第二个则必须是String类型   </p>
<p>   注意在使用之前需要在xml文件的beans标签中加入新的schame文件:并在Eclipse中进行关联配置<br>   <beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop" xmlns:context="http://www.springframework.org/schema/context" xmlns:tx="http://www.springframework.org/schema/tx" xsi:schemalocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context-3.2.xsd
           http://www.springframework.org/schema/aop
           http://www.springframework.org/schema/aop/spring-aop-3.2.xsd"></beans></p>
<p>   下面给出一些常见切入点表达式的例子。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">1）任意包下的任意类中的公共方法的执行：  </span><br><span class="line">   execution(public **(..))</span><br><span class="line">2）任何一个以“set”开始的方法的执行：  </span><br><span class="line">   execution(* set*(..))</span><br><span class="line">3）AccountService 接口的任意方法的执行：    </span><br><span class="line">   execution(* com.briup.service.AccountService.*(..))</span><br><span class="line">4）定义在service包里的任意方法的执行：  </span><br><span class="line">   execution(* com.briup.service.*.*(..))</span><br><span class="line">5）定义在service包或者子包里的任意方法的执行：  </span><br><span class="line">   execution(* com.briup.service..*.*(..))</span><br><span class="line">6）在service包里的任意连接点（在Spring AOP中只是方法执行）</span><br><span class="line">   within(com.xyz.service.*)</span><br><span class="line">7）在service包或者子包里的任意连接点（在Spring AOP中只是方法执行） ：  </span><br><span class="line">   within(com.xyz.service..*)</span><br></pre></td></tr></table></figure></p>
<p>   注意:  1.从spring容器中拿代理对象的时候也是要用目标对象的名字来拿。<br>      2.没有实现任何接口的目标对象也能产生代理对象。<br>      3.proxy-target-class=”true”.放在<aop:config>标签中。<br>      默认是false:目标类有接口则使用JDK动态代理。没有实现接口就用CGLib的方式。<br>      如果为true:目标类无论有无实现接口都要使用CGLib类来产生动态代理对象      </aop:config></p>
<pre><code>    &lt;!-- 配置aop的代理 --&gt;
&lt;aop:config&gt;
    &lt;!-- 定义一个切入点 并给切入点起名为myPointCut --&gt;
    &lt;!-- 切入点是一组连接点的集合 --&gt;
    &lt;aop:pointcut expression=&quot;execution(public * com.briup.aop.service.*.*(..))&quot; id=&quot;myPointCut&quot;/&gt;

    &lt;!-- 定义哪一个advice在哪一个切入点上面起作用 --&gt;
    &lt;aop:advisor advice-ref=&quot;beforeAdvice&quot; pointcut-ref=&quot;myPointCut&quot; /&gt;
&lt;/aop:config&gt;
&lt;!-- 
    expression=&quot;execution(public * com.briup.aop.service.*.*(..))&quot;
    这个引号&quot;&quot;里面就是用表达式的方式来定义切入点,只要是符合我们这个表达式要求的
    方法就是我们的连接点,连接点的集合就是我们要定义的切入点。
    表达式中从左到右的*号:
        第一个* 表示方法的返回类型不限。
        第二个* 表示包中的任意一个类
        第三个* 表示类中的任意一个方法

    同时方法的参数也没有限制.
 --&gt;
</code></pre><p>5  在一个切面类中定个多个方法,根据xml文件的配置每个方法都可以织入到切入点的不同位置,并且advice是在aop的标签中进行配置,不需要再写对应的advice类了<br>   例如:<br>    //这个类相当于我们之前的切面类<br>    //只不过这个切面类中有很多方法都可以织入到切入点上面<br>    //我们可以控制把这里的任何一个方法织入到任何一个切入点上面<br>    public class XmlHandler {</p>
<pre><code>    public void beforeTest(JoinPoint p){
        System.out.println(p.getSignature().getName()+&quot; before...&quot;);
    }


    public void afterTest(JoinPoint p){
        System.out.println(p.getSignature().getName()+&quot; after...&quot;);
    }

    public void afterReturningTest(JoinPoint p){

        System.out.println(p.getSignature().getName()+&quot; afterReturning&quot;);

    }

    //在和aroundAdvice结合的时候,这个方法一定要加上这个ProceedingJoinPoint类型的参数
    public Object aroundTest(ProceedingJoinPoint pjp)throws Throwable{
        //JoinPoint对象不能调用连接点所表示的方法 
        //ProceedingJoinPoint能调用连接点所表示的方法 pjp.proceed()
        System.out.println(pjp.getSignature().getName()+&quot; is start..&quot;);
        //调用到连接点方法 
        Object obj = pjp.proceed(); 
        System.out.println(pjp.getSignature().getName()+&quot; is end..&quot;);
        return obj;
    }

    public void throwingTest(JoinPoint p,Exception ex){
        System.out.println(p.getSignature().getName()+&quot; is throwing...&quot;+ex.getMessage());

    }
}

    xml文件配置:
&lt;!-- 配置dao层对象 --&gt;
&lt;bean id=&quot;dao&quot; 
    class=&quot;com.briup.aop.dao.AccountDaoImpl&quot;/&gt;
 &lt;!-- 配置目标对象 --&gt;
&lt;bean name=&quot;target&quot; 
class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
        &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;
&lt;!-- 配置切面类 里面包含了很多不同的切面 --&gt;
&lt;!-- 我们需要把这个切面类中的不同切面织入到织入点中的不同方法中 --&gt;
&lt;bean name=&quot;handler&quot; class=&quot;com.briup.aop.xml.XmlHandler&quot;&gt;&lt;/bean&gt;

&lt;!-- 配置aop的代理 --&gt;
&lt;aop:config&gt;
    &lt;!-- 定义切入点名为myPointCut --&gt;
    &lt;aop:pointcut expression=&quot;execution(public * com.briup.aop.service.*.*(..))&quot; id=&quot;myPointCut&quot;/&gt;

    &lt;!-- 定义切面类 以及需要使用的advice --&gt;
    &lt;aop:aspect id=&quot;aspect&quot; ref=&quot;handler&quot;&gt;
        &lt;!-- 表示beforeAdvice会把切面类handler中的beforeTest方法织入到名字叫myPointCut的切入点上面 --&gt;
        &lt;aop:before method=&quot;beforeTest&quot; pointcut-ref=&quot;myPointCut&quot;/&gt;

        &lt;!-- after表示不管方法是否正常结束都会起作用 --&gt;
        &lt;aop:after method=&quot;afterTest&quot; pointcut-ref=&quot;myPointCut&quot;/&gt;

        &lt;!-- after-returning表示方法正常结束才会起作用(抛异常时候不起作用) --&gt;
        &lt;aop:after-returning method=&quot;afterReturningTest&quot; pointcut-ref=&quot;myPointCut&quot;/&gt;

        &lt;aop:around method=&quot;aroundTest&quot; pointcut-ref=&quot;myPointCut&quot;/&gt;

        &lt;!-- throwing=&quot;ex&quot;表示throwingTest方法中接收异常对象的名字一定要是ex --&gt;
        &lt;aop:after-throwing method=&quot;throwingTest&quot; pointcut-ref=&quot;myPointCut&quot; throwing=&quot;ex&quot;/&gt;  
        &lt;!-- throwing 指定的ex  要和throwingTest 方法中的形参名字一致  --&gt;

    &lt;/aop:aspect&gt;
&lt;/aop:config&gt;

注意:&lt;aop:config proxy-target-class=&quot;true&quot;&gt; 如果这样配置则是强制使用CGLIB方式进行代理
</code></pre><p>6  使用注解配置AOP:其实就是在上面的类XmlHandler中加入上注解然后去掉xml中的aop标签配置,这里把类改名为AnnotationHandler,<br>   例子:<br>    @Component<br>    @Aspect<br>    public class AnnotationHandler {<br>        /*</p>
<pre><code>     * 在一个方法上面加上注解来定义切入点
     * 这个切入点的名字就是这个方法的名字
     * 这个方法本身不需要有什么作用
     * 这个方法的意义就是:给这个 @Pointcut注解一个可以书写的地方
     * 因为注解只能写在方法、属性、类的上面,并且方法名作为切入点的名字
     * */
    @Pointcut(&quot;execution(public * com.briup.aop.service..*.*(..))&quot;)
    public void myPointCut(){}

    //注:这里面的所有方法的JoinPoint类型对象都可以去掉不写
    @Before(&quot;myPointCut()&quot;)
    public void beforeTest(JoinPoint p){
        System.out.println(p.getSignature().getName()+&quot; before...&quot;);
    }


    /*
     * @After和@AfterReturning
     * 
     * @After标注的方法会在切入点上的方法结束后被调用(不管是不是正常的结束).
     * @AfterReturning标注的方法只会在切入点上的方法正常结束后才被调用.
     * */
    @After(&quot;myPointCut()&quot;)
    public void afterTest(JoinPoint p){
        System.out.println(p.getSignature().getName()+&quot; after...&quot;);
    }
    @AfterReturning(&quot;myPointCut()&quot;)
    public void afterReturningTest(JoinPoint p){

        System.out.println(p.getSignature().getName()+&quot; afterReturning&quot;);

    }

    @Around(&quot;myPointCut()&quot;)
    public Object aroundTest(ProceedingJoinPoint pjp)throws Throwable{
        System.out.println(pjp.getSignature().getName()+&quot; is start..&quot;);
        //调用连接点的方法去执行
        Object obj = pjp.proceed();
        System.out.println(pjp.getSignature().getName()+&quot; is end..&quot;);
        return obj;
    }



    //在切入点中的方法执行期间抛出异常的时候,会调用这个 @AfterThrowing注解所标注的方法
    @AfterThrowing(value=&quot;myPointCut()&quot;,throwing=&quot;ex&quot;)
    public void throwingTest(JoinPoint p,Exception ex){
        System.out.println(p.getSignature().getName()+&quot; is throwing...&quot;+ex.getMessage());

    }


}

xml配置:注意给例子中使用的其他的类上面也使用注解
&lt;aop:aspectj-autoproxy/&gt;
&lt;context:component-scan base-package=&quot;com.briup.aop&quot;/&gt;

注意:&lt;aop:aspectj-autoproxy proxy-target-class=&quot;true&quot;/&gt;这样配置则是强制使用CGLIB进行代理
</code></pre>]]></content>
      
        <categories>
            
            <category> Java </category>
            
        </categories>
        
        
        <tags>
            
            <tag> Java </tag>
            
            <tag> Spring </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[Spring入门(二)]]></title>
      <url>//blog/2018/01/25/Spring%E5%85%A5%E9%97%A8-%E4%BA%8C/</url>
      <content type="html"><![CDATA[<h2 id="代理模式"><a href="#代理模式" class="headerlink" title="代理模式"></a>代理模式</h2><p>   代理模式是常用的Java设计模式，它的特征是代理类与委托类有同样的接口，代理类主要负责为委托类预处理消息、过滤消息、把消息转发给委托类，以及事后处理消息等。<br>   代理类与委托类之间通常会存在关联关系，一个代理类的对象与一个委托类的对象关联，代理类的对象本身并不真正实现服务，而是通过调用委托类的对象的相关方法，来提供特定的服务。<br>   注意:<br>      委托类对象就是我们后面说到的 目标对象(需要【被】代理的对象)<br>      代理类对象就是我们后面说到的 代理对象(目标对象就是需要这个对象做为代理)<br>   按照代理类的创建时期，代理类可分为两种。  </p>
<blockquote>
<ol>
<li>静态代理类:<br>由程序员创建或由特定工具自动生成源代码，再对其编译。在程序运行前，代理类的.class文件就已经存在了  </li>
<li>动态代理类：在程序运行时，运用反射机制动态创建而成。<br><img src="https://images2015.cnblogs.com/blog/776259/201606/776259-20160618235740620-555209026.png" alt="Spring"><a id="more"></a>
</li>
</ol>
</blockquote>
<h3 id="静态代理"><a href="#静态代理" class="headerlink" title="静态代理"></a>静态代理</h3><p>   例如:<br>    接口：HelloService<br>    委托类：HelloServiceImpl<br>    代理类：HelloServiceProxy</p>
<pre><code>public interface HelloService{
  public String echo(String msg);
  public Date getTime();
}

public class HelloServiceImpl implements HelloService{
  public String echo(String msg){
    return &quot;echo:&quot;+msg;
  }
  public Date getTime(){
   return new Date();
  }
}

public class HelloServiceProxy implements HelloService{
  private HelloService helloService; //表示被代理的HelloService 实例
  public HelloServiceProxy(HelloService helloService){
    this.helloService=helloService;
  }
  public void setHelloServiceProxy(HelloService helloService){
     this.helloService=helloService;
  }
  public String echo(String msg){
    System.out.println(&quot;before calling echo()&quot;); //目标方法调前处理
    //调用委托类对象的方法(也就是目标对象方法/被代理对象方法)
    //这个方法才是我们真正要执行的方法
    String result=helloService.echo(msg); 
    System.out.println(&quot;after calling echo()&quot;); //目标方法调用后处理
    return result;
  }
  public Date getTime(){
    System.out.println(&quot;before calling getTime()&quot;); //目标方法调前处理

    //调用委托类对象的方法(也就是目标对象方法/被代理对象方法)
    //这个方法才是我们真正要执行的方法
    Date date=helloService.getTime();

    System.out.println(&quot;after calling getTime()&quot;); //目标方法调用后处理
    return date;
   }
}

main:
   HelloService helloService=new HelloServiceImpl();
   HelloService helloServiceProxy=new HelloServiceProxy(helloService);
   System.out.println(helloServiceProxy.echo(&quot;hello&quot;));
</code></pre><h3 id="动态代理"><a href="#动态代理" class="headerlink" title="动态代理"></a>动态代理</h3><p>   与静态代理类对照的是动态代理类，动态代理类的字节码在程序运行时由Java反射机制动态生成，无需程序员手工编写它的源代码。动态代理类不仅简化了编程工作，而且提高了软件系统的可扩展性，因为Java 反射机制可以生成任意类型的动态代理类。java.lang.reflect 包下面的<strong>Proxy类</strong>和<strong>InvocationHandler 接口</strong>提供了生成动态代理类的能力。<br>   (动态代理需要依赖接口,cglib代理是通过继承完成代理)<br>   例子：<br>   接口:<br>   <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">public interface IStudentService &#123;</span><br><span class="line"></span><br><span class="line"> void save(Student s);</span><br><span class="line"></span><br><span class="line"> void delete(long id);</span><br><span class="line"></span><br><span class="line"> Student find(long id);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>   实现类<br>   <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">public class StudentServiceImpl implements IStudentService &#123;</span><br><span class="line"> public void delete(long id) &#123;</span><br><span class="line">     // 记录日志。。。</span><br><span class="line">     System.out.println(&quot;student is deleted...&quot;);</span><br><span class="line"> &#125;</span><br><span class="line"> public Student find(long id) &#123;</span><br><span class="line">     // 记录日志。。。</span><br><span class="line">     System.out.println(&quot;student is found...&quot;);</span><br><span class="line">     return null;</span><br><span class="line"> &#125;</span><br><span class="line"> public void save(Student s) &#123;</span><br><span class="line">     // 记录日志。。。</span><br><span class="line">     System.out.println(&quot;student is saved...&quot;);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
   <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line">日志类:</span><br><span class="line">public class StudentLogger &#123;</span><br><span class="line"> public void log(String msg)&#123;</span><br><span class="line">     System.out.println(&quot;log: &quot;+msg);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br><span class="line">//InvocationHandler接口的实现类,java的动态代理中需要使用</span><br><span class="line">public class MyHandler implements InvocationHandler &#123;</span><br><span class="line"> //目标对象</span><br><span class="line"> private Object target; </span><br><span class="line"> private StudentLogger logger = new StudentLogger();</span><br><span class="line"></span><br><span class="line"> public MyHandler() &#123;</span><br><span class="line"> &#125;</span><br><span class="line"> public MyHandler(Object target) &#123;</span><br><span class="line">     this.target = target;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> // 参数1 将来所产生的代理对象 Proxy4$</span><br><span class="line"> // 参数2 将来需要调用到的目标对象里面真正的那个方法的镜像</span><br><span class="line"> // 参数3 将来调用方法的时候所传的参数</span><br><span class="line"> public Object invoke(Object proxy, Method m, Object[] args)</span><br><span class="line">         throws Throwable &#123;</span><br><span class="line">     // 获得将来所调用方法的名字</span><br><span class="line">     String methodName = m.getName();</span><br><span class="line">     // 用日志记录输出一下</span><br><span class="line">     logger.log(methodName + &quot; is invoked...&quot;);</span><br><span class="line">     // 用反射的方式去调用将来需要真正调用的方法.</span><br><span class="line">     Object o = m.invoke(target, args);</span><br><span class="line"></span><br><span class="line">     return o;</span><br><span class="line"> &#125;</span><br><span class="line"> get/set</span><br><span class="line"> ....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line">   main:</span><br><span class="line">    //目标对象</span><br><span class="line">    IStudentService service = new StudentServiceImpl();</span><br><span class="line">    //service是我们的目标对象。</span><br><span class="line">    //我们要给目标对象产生代理对象。</span><br><span class="line">    //目标对象service只能单独执行delete方法。</span><br><span class="line">    //但是我们需要的是:先执行log日志方法再执行delete方法。</span><br><span class="line">    //目标对象service做不到这个要求,所以我们要给目标对象service</span><br><span class="line">    //生成一个代理对象去完成这俩个操作.</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    //怎么给目标对象生成代理对象:</span><br><span class="line">    //JDK动态代理的方式</span><br><span class="line"></span><br><span class="line">    //获得目标对象的Class对象</span><br><span class="line">    Class c = service.getClass();</span><br><span class="line">    //获得目标对象的类加载器对象</span><br><span class="line">    ClassLoader classLoader = c.getClassLoader();</span><br><span class="line"></span><br><span class="line">    //获得目标对象所实现的所有接口</span><br><span class="line">    Class[] interfaces = c.getInterfaces();</span><br><span class="line"></span><br><span class="line">    //获得一个InvocationHandler接口的实现类对象,并把目标对象传进去</span><br><span class="line">    InvocationHandler h = </span><br><span class="line">            new MyHandler(service);</span><br><span class="line"></span><br><span class="line">    //参数1 目标对象的类加载器对象</span><br><span class="line">    //参数2 目标对象所实现的所有接口. Class类型数组</span><br><span class="line">    //参数3 InvocationHandler接口的实现类对象</span><br><span class="line">    IStudentService proxy = </span><br><span class="line">        (IStudentService)Proxy.newProxyInstance</span><br><span class="line">        (classLoader, interfaces, h);</span><br><span class="line">    //这里的proxy是一个实现了IStudentService接口动态生成的代理类的对象</span><br><span class="line">    proxy.delete();</span><br><span class="line"></span><br><span class="line">注意：getClass()方法</span><br><span class="line">      toString()方法的区别</span><br><span class="line">   在invoke方法中调用代理对象的这两个方法</span><br><span class="line">   在main方法中调用代理对象的这两个方法</span><br></pre></td></tr></table></figure>
<ol>
<li><p>CGLib代理<br>JDK实现动态代理需要实现类通过接口定义业务方法，对于没有接口的类，如何实现动态代理呢，这就需要CGLib了。CGLib采用了非常底层的字节码技术，其原理是通过字节码技术为目标对象创建一个子类对象，并在子类对象中拦截所有父类方法的调用，然后在方法调用前后调用后都可以加入自己想要执行的代码。JDK动态代理与CGLib动态代理都是Spring AOP的采用的代理方式。</p>
<p>简单的实现：<br>这是一个需要被代理的类，也就是父类，通过字节码技术创建这个类的子类，实现动态代理。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">public class SayHello &#123;</span><br><span class="line">  public void say()&#123;</span><br><span class="line">      System.out.println(&quot;hello everyone&quot;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>注意:在cglib方式中,目标对象作为父类,代理对象作为目标对象动态生成的子类对象</p>
<p>该类实现了创建一个类的子类的方法(cglib给一个类生成代理对象的方式)<br>getProxy(SuperClass.class)方法通过参数即父类的class对象，创建出它的一个子类对象,也就是cglib方式的代理对象    </p>
<blockquote>
<p>  intercept()方法拦截所有目标类方法的调用，<br>obj表示委托类对象<br>method为委托类中方法的反射对象，args为方法的动态入参，<br>mproxy为代理类(子类)中方法的反射对象。<br>mproxy.invokeSuper(obj, args)通过代理类调用目标对象(父类)中的方法。  </p>
</blockquote>
<p>//在当前类创建目标对象的代理对象(目标对象的子类对象)</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">public class CglibProxy implements MethodInterceptor&#123;</span><br><span class="line"></span><br><span class="line">  //返回目标对象的代理对象</span><br><span class="line"> public Object getProxy(Class clazz)&#123;</span><br><span class="line">     //可以动态生成目标对象的代理对象</span><br><span class="line">      Enhancer enhancer = new Enhancer();</span><br><span class="line">      //设置谁是父类</span><br><span class="line">      enhancer.setSuperclass(clazz);</span><br><span class="line">        //拦截住该方法调用的方法是谁</span><br><span class="line">         //this代表当前类的intercept方法</span><br><span class="line">      enhancer.setCallback(this);</span><br><span class="line">      //通过字节码技术动态创建子类实例</span><br><span class="line">      return enhancer.create();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  //实现MethodInterceptor接口方法</span><br><span class="line">  public Object intercept(Object obj, Method method, Object[] args,</span><br><span class="line">      MethodProxy mproxy) throws Throwable &#123;</span><br><span class="line">      System.out.println(&quot;前置代理&quot;);</span><br><span class="line">      //通过代理类调用父类中的方法</span><br><span class="line">      //即会执行到我们目标对象中的方法</span><br><span class="line">      Object result = mproxy.invokeSuper(obj, args);</span><br><span class="line">      System.out.println(&quot;后置代理&quot;);</span><br><span class="line">      return result;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">main:</span><br><span class="line">   CglibProxy proxy = new CglibProxy();</span><br><span class="line">   //通过生成子类的方式创建代理类</span><br><span class="line">   SayHello proxyImp = (SayHello)proxy.getProxy(SayHello.class);</span><br><span class="line">   proxyImp.say();</span><br></pre></td></tr></table></figure>
<p>   输出结果：<br>   前置代理<br>   hello everyone<br>   后置代理</p>
<ol>
<li><p>Spring实现AOP(Aspect Oriented   Programming)是依赖JDK动态代理和CGLIB代理(不同情况spring会自己选择一种方式)。<br>JDK动态代理和CGLIB代理的对比:<br>JDK动态代理：其代理对象必须是某个接口的实现，它是通过在运行期间创建一个接口的实现类来完成对目标对象的代理。<br>CGLIB代理：实现原理类似于JDK动态代理，只是它在运行期间生成的代理对象是针对目标类扩展的子类。CGLIB是高效的代码生成包，底层是依靠ASM（开源的java字节码编辑类库）操作字节码实现的。    </p>
<p>所以spring会有以下俩种选择动态代理实现方式的情况:<br> <em>如果目标对象实现了接口，默认情况下会采用JDK的动态代理实现AOP    
 </em>如果目标对象没有实现了接口，必须采用CGLIB库，spring会自动在JDK动态代理和CGLIB之间自动选择;    </p>
</li>
</ol>
<p>7  认识AOP中的一些基本概念,然后在一个一个的例子中,不断的加强对这些概念的理解,同时要能自己表述出每个概念的含义  </p>
<pre><code>AOP          面向切面编程

aspect        切面/切面类

joinPoint    连接点
        在spring的aop中只有类(目标对象)中的方法 可以做连接点,每一个方法都可以是一个连接点.

pointCut    切入点 
        一组连接点的集合

advice        通知/拦截器 
        用来控制切面类将来到底是织入到切入点的前面、后面或者是抛异常的时候。

adivsor        增强器
        用来筛选类中的哪些方法是我们的连接点(哪些方法需要被拦截).

target         目标对象

proxy        代理对象

wave        织入：在运行期间advice将切面动态插入到切入点中去的过程。

    前置通知（Before advice）： 
        在某连接点（join point）之前执行的通知

    返回后通知（After returning advice）： 
        在某连接点（join point）正常完成后执行的通知：例如，一个方法没有抛出任何异常，正常返回。

    抛出异常后通知（After throwing advice）： 
        在方法抛出异常退出时执行的通知。

    环绕通知（Around Advice）： 
        包围一个连接点（join point）的通知，例如事务的处理,就需要这样的通知,因为事务需要在方法前开启,在方法后提交
</code></pre><p>7  在Spring中，<strong>Advice是由spring中的几个接口</strong>来指定(就像action类由struts2中的action接口来指定一样)，主要有以下几种：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">1) Before Advice   </span><br><span class="line">public interface MethodBeforeAdvice extends BeforeAdvice &#123;   </span><br><span class="line">    void before(Method m, Object[] args, Object target) throws Throwable;   </span><br><span class="line">&#125;     </span><br><span class="line">例如:   </span><br><span class="line">//有一下几个类或者接口:</span><br></pre></td></tr></table></figure></p>
<pre><code>Account.java  
    private int id;   
    private String name;  
    private double balance;//余额  
    get/set
    ...
AccountDao.java
    //取款 账号减去多少钱
    void withdraw(Account acc,double amt);
    //存款 账号加上多少钱
    void deposit(Account acc,double amt);
    <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"> </span><br></pre></td></tr></table></figure>

AccountDaoImpl.java
    //简单的实现接口中的抽象方式

IAccountService.java
    //银行账号的一个操作:例如转账
    void bankAction();

AccountServiceImpl.java
    private AccountDao accountDao;
    private Account account;
    //转账
    public void bankAction(){
        accountDao.withdraw(account, 100);
        accountDao.deposit(account, 100);
    }
    get/set
    ...

//切面类
public class MyLogger {
    public void log(String msg){
        System.out.println(&quot;log:&quot;+msg);
    }
}

/*我们要做的事情:在转账方法(bankAction)执行之前进行一个日志输出*/

//前置通知
public class BeforeAdvice implements MethodBeforeAdvice {
    //切面类
    private MyLogger logger;

    // 参数1 将来我们需要调用的目标对象中的方法镜像
    // 参数2 将来调用方法的时候所传过来的参数
    // 参数3 目标对象
    //将来在调用目标对象方法之前,会先执行这个before方法
    public void before(Method m, Object[] args, Object target) throws Throwable {

        logger.log(m.getName() + &quot; is invoked..&quot;);

        /*
         * 注意:这里一定不要自己手动的用反射去 调用这个目标对象中的方法,
         * 因为spring 会帮我们去调用的,如果我们这个再去调用这个方法,
         * 那么这这个方法会被调用俩次.
         * 
         * m.invoke(target,args);
         * 
         */
    }
    get/set
}

配置xml文件: 注意ProxyFactoryBean的配置,htmlsingle中搜索即可
    &lt;!-- 配置切面类 --&gt;
    &lt;bean name=&quot;logger&quot; class=&quot;com.briup.aop.aspect.MyLogger&quot;&gt;&lt;/bean&gt;

    &lt;!-- 配置advice --&gt;
    &lt;bean name=&quot;beforeAdvice&quot; 
    class=&quot;com.briup.aop.before.BeforeAdvice&quot;&gt;
    &lt;!-- 注入切面类对象 --&gt;
    &lt;property name=&quot;logger&quot; ref=&quot;logger&quot;&gt;&lt;/property&gt;
    &lt;/bean&gt;

    &lt;!-- 配置dao层对象 --&gt;
    &lt;bean id=&quot;dao&quot; class=&quot;com.briup.aop.dao.AccountDaoImpl&quot;/&gt;

    &lt;!-- 配置目标对象 --&gt;
    &lt;bean name=&quot;target&quot; 
    class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
    &lt;property name=&quot;account&quot; ref=&quot;account&quot;&gt;&lt;/property&gt;
    &lt;/bean&gt;
    &lt;bean name=&quot;account&quot; class=&quot;com.briup.aop.pojo.Account&quot;&gt;
    &lt;property name=&quot;id&quot; value=&quot;1&quot;&gt;&lt;/property&gt;    
        &lt;property name=&quot;name&quot; value=&quot;tom&quot;&gt;&lt;/property&gt;    
        &lt;property name=&quot;balance&quot; value=&quot;1000&quot;&gt;&lt;/property&gt;    
 &lt;/bean&gt;
    &lt;!-- 配置代理对象 --&gt;
    &lt;!-- 这里使用的是spring的一个代理对象工厂类产生的 --&gt;
    &lt;bean name=&quot;proxy&quot; 
    class=&quot;org.springframework.aop.framework.ProxyFactoryBean&quot;&gt;
    &lt;!-- 注入目标对象 --&gt;
    &lt;property name=&quot;target&quot; ref=&quot;target&quot;&gt;&lt;/property&gt;

    &lt;!-- 注入目标对象所实现的接口 可以有多个接口 --&gt;
    &lt;property name=&quot;proxyInterfaces&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;com.briup.aop.service.IAccountService&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;

    &lt;!-- 注入advice  可以有多个 --&gt;
    &lt;property name=&quot;interceptorNames&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;beforeAdvice&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;

    &lt;/bean&gt;
</code></pre><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<pre><code>2) After Returning advice
public interface AfterReturningAdvice extends Advice {
    void afterReturning(Object returnValue, Method m, Object[] args, Object target) throws Throwable;
}

例如:
public class AfterAdvice implements AfterReturningAdvice {
    private MyLogger logger;

    //参数1 目标对象中的方法执行完返回值
    //参数2 所执行方法的镜像对象
    //参数3 执行方法时候所传的参数
    //参数4 目标对象
    //将来调用目标对象的方法之后会执行这个afterReturning方法
    public void afterReturning(Object returnValue, Method method,
            Object[] args, Object target) throws Throwable {

        logger.log(&quot;after returning &quot; + &quot; target=&quot; + target
                + &quot; method Name=&quot; + method.getName() + &quot; args are:&quot; + args
                + &quot; returnValue=&quot; + returnValue);
    }
    get/set
}

xml配置文件:
&lt;!-- 配置切面类 --&gt;
&lt;bean name=&quot;logger&quot; class=&quot;com.briup.aop.aspect.MyLogger&quot;&gt;&lt;/bean&gt;

&lt;!-- 配置advice --&gt;
&lt;bean name=&quot;afterAdvice&quot;
    class=&quot;com.briup.aop.after.AfterAdvice&quot;&gt;
    &lt;property name=&quot;logger&quot; ref=&quot;logger&quot;&gt;&lt;/property&gt;    
&lt;/bean&gt;

&lt;!-- 配置dao层对象 --&gt;
&lt;bean id=&quot;dao&quot; class=&quot;com.briup.aop.dao.AccountDaoImpl&quot; /&gt;

&lt;!-- 配置目标对象 --&gt;
&lt;bean name=&quot;target&quot;
    class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;

&lt;!-- 配置代理对象 --&gt;
&lt;!-- 这里使用的是spring的一个代理对象工厂类产生的 --&gt;
&lt;bean name=&quot;proxy&quot;
    class=&quot;org.springframework.aop.framework.ProxyFactoryBean&quot;&gt;
    &lt;!-- 注入目标对象 --&gt;
    &lt;property name=&quot;target&quot; ref=&quot;target&quot;&gt;&lt;/property&gt;

    &lt;!-- 注入目标对象所实现的接口 可以有多个接口 --&gt;
    &lt;property name=&quot;proxyInterfaces&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;com.briup.aop.service.IAccountService&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;

    &lt;!-- 注入advice  可以有多个 --&gt;
    &lt;property name=&quot;interceptorNames&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;afterAdvice&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;

&lt;/bean&gt;

注意:另外一个返回后通知接口:AfterAdvice的使用方式和这个是类似的,但是需要注意它们俩个之间的区别

3) 环绕Advice：
public interface MethodInterceptor extends Interceptor {
      Object invoke(MethodInvocation invocation) throws Throwable;
}

例如:
public class AroundAdvice implements MethodInterceptor {
private MyLogger logger;

public Object invoke(MethodInvocation mi) throws Throwable {
    // mi.getMethod()获得将来要调用的方法的镜像
    //在目标方法执行之前做日志
    logger.log(mi.getMethod().getName() + &quot; is start...&quot;);

    // 这个方法就是用来调用目标对象中的方法的
    Object returnValue = mi.proceed();

    //在目标方法执行之后做日志
    logger.log(mi.getMethod().getName() + &quot; is end...&quot;);

    return returnValue;
}
get/set

xml配置文件:
    &lt;!-- 配置切面类 --&gt;
    &lt;bean name=&quot;logger&quot; class=&quot;com.briup.aop.aspect.MyLogger&quot;&gt;&lt;/bean&gt;

    &lt;!-- 配置advice --&gt;
    &lt;bean name=&quot;aroundAdvice&quot; 
    class=&quot;com.briup.aop.around.AroundAdvice&quot;&gt;
    &lt;!-- 注入切面类对象 --&gt;
    &lt;property name=&quot;logger&quot; ref=&quot;logger&quot;&gt;&lt;/property&gt;
    &lt;/bean&gt;

    &lt;!-- 配置dao层对象 --&gt;
    &lt;bean name=&quot;dao&quot; class=&quot;com.briup.aop.dao.AccountDaoImpl&quot;/&gt;

    &lt;!-- 配置目标对象 --&gt;
    &lt;bean name=&quot;target&quot; 
    class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
    &lt;/bean&gt;

    &lt;!-- 配置代理对象 --&gt;
    &lt;!-- 这里使用的是spring的一个代理对象工厂类产生的 --&gt;
    &lt;bean name=&quot;proxy&quot; 
    class=&quot;org.springframework.aop.framework.ProxyFactoryBean&quot;&gt;
    &lt;!-- 注入目标对象 --&gt;
    &lt;property name=&quot;target&quot; ref=&quot;target&quot;&gt;&lt;/property&gt;

    &lt;!-- 注入目标对象所实现的接口 可以有多个接口 --&gt;
    &lt;property name=&quot;proxyInterfaces&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;com.briup.aop.service.IAccountService&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;

    &lt;!-- 注入advice 可以有多个 --&gt;
    &lt;property name=&quot;interceptorNames&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;aroundAdvice&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;
    &lt;/bean&gt;


4) Throws Advice
//ThrowsAdvice 是一个空接口，起标识作用
public interface ThrowsAdvice extends Advice {

}

例如:
public class ThrowingAdvice implements ThrowsAdvice {
    private MyLogger logger;

    public MyLogger getLogger() {
        return logger;
    }

    public void setLogger(MyLogger logger) {
        this.logger = logger;
    }

    //这里这个方法的名字一定要叫afterThrowing
    //参数可以是1个也可以是四个
    //1个参数的时候只能是一个异常类型的参数
    //如果是4个参数的话,参数的顺序也一定要是下面的顺序
    public void afterThrowing(Method method, Object[] args, Object target,Exception e) {
        logger.log(e.getMessage());
    }

    //下面这样写也可以
    /*
    public void afterThrowing(Exception e) {

        logger.log(e.getMessage());
    }
    */
    get/set
}

配置xml文件:
    &lt;!-- 配置切面类 --&gt;
    &lt;bean name=&quot;logger&quot; class=&quot;com.briup.aop.aspect.MyLogger&quot;&gt;&lt;/bean&gt;

    &lt;!-- 配置advice --&gt;
    &lt;bean name=&quot;throwAdvice&quot; class=&quot;com.briup.aop.throwException.ThrowingAdvice&quot;&gt;
    &lt;!-- 注入切面类对象 --&gt;
    &lt;property name=&quot;logger&quot; ref=&quot;logger&quot;&gt;&lt;/property&gt;
    &lt;/bean&gt;

    &lt;!-- 配置dao层对象 --&gt;
    &lt;bean id=&quot;dao&quot; class=&quot;com.briup.aop.dao.AccountDaoImpl&quot;/&gt;

    &lt;!-- 配置目标对象 --&gt;
    &lt;bean name=&quot;target&quot; 
    class=&quot;com.briup.aop.service.AccountServiceImpl&quot;&gt;
    &lt;property name=&quot;accountDao&quot; ref=&quot;dao&quot;&gt;&lt;/property&gt;
    &lt;/bean&gt;

    &lt;!-- 配置代理对象 --&gt;
    &lt;!-- 这里使用的是spring的一个代理对象工厂类产生的 --&gt;
    &lt;bean name=&quot;proxy&quot; 
    class=&quot;org.springframework.aop.framework.ProxyFactoryBean&quot;&gt;
    &lt;!-- 注入目标对象 --&gt;
    &lt;property name=&quot;target&quot; ref=&quot;target&quot;&gt;&lt;/property&gt;

    &lt;!-- 注入目标对象所实现的接口 可以有多个接口 --&gt;
    &lt;property name=&quot;proxyInterfaces&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;com.briup.aop.service.IAccountService&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;

    &lt;!-- 注入advice  可以有多个 --&gt;
    &lt;property name=&quot;interceptorNames&quot;&gt;
        &lt;list&gt;
            &lt;value&gt;throwAdvice&lt;/value&gt;
        &lt;/list&gt;
    &lt;/property&gt;
    &lt;/bean&gt;
</code></pre>]]></content>
      
        <categories>
            
            <category> Java </category>
            
        </categories>
        
        
        <tags>
            
            <tag> Java </tag>
            
            <tag> Spring </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[Spring概念简记]]></title>
      <url>//blog/2018/01/25/Spring%E6%A6%82%E5%BF%B5%E7%AE%80%E8%AE%B0/</url>
      <content type="html"><![CDATA[<h1 id="Spring-概念简记"><a href="#Spring-概念简记" class="headerlink" title="Spring 概念简记"></a>Spring 概念简记</h1><p><img src="https://www.ibm.com/developerworks/cn/java/wa-spring1/spring_framework.gif" alt="Spring 组件"></p>
<a id="more"></a>
<h2 id="什么是代理模式？静态代理和动态代理有什么区别？"><a href="#什么是代理模式？静态代理和动态代理有什么区别？" class="headerlink" title="什么是代理模式？静态代理和动态代理有什么区别？"></a>什么是代理模式？静态代理和动态代理有什么区别？</h2><p>代理模式是常用的Java设计模式，它的特征是代理类与委托类有同样的接口，<br>代理类主要负责为委托类预处理消息、过滤消息、把消息转发给委托类，以及事后处理消息等。<br>代理类与委托类之间通常会存在关联关系，一个代理类的对象与一个委托类的对象关联，<br>代理类的对象本身并不真正实现服务，而是通过调用委托类的对象的相关方法，来提供特定的服务。</p>
<p><strong>静态代理:</strong><br>由程序员创建或由特定工具自动生成源代码，再对其编译。在程序运行前，代理类的.class文件就已经存在了。<br><strong>动态代理类：</strong><br>在程序运行时，运用反射机制动态创建而成。</p>
<p> 动态代理类的字节码在程序运行时由Java反射机制动态生成，无需程序员手工编写它的源代码。<br> 动态代理类不仅简化了编程工作，而且提高了软件系统的可扩展性，因为Java 反射机制可以生成任意类型的动态代理类。java.lang.reflect 包下面的Proxy类和InvocationHandler 接口提供了生成动态代理类的能力。</p>
<h2 id="动态代理的实现步骤如何？"><a href="#动态代理的实现步骤如何？" class="headerlink" title="动态代理的实现步骤如何？"></a>动态代理的实现步骤如何？</h2><pre><code>1. 委托类实现接口
2. 代理类中维护委托类对象  Target target  
3. 通过Proxy.newProxyInstance(              //通过 Proxy 静态方法获取代理对象
          target.geClass().getClassLoder()  , // 委托类类加载器
          target.getClass().getInterfaces(),   //  委托类接口字节码类 数组对象
          new InvocationHandler(              // 匿名内部类  实现接口
                Object proxy,                        // 返回的代理实例  不要在InvacationHandler 中调用该对象         
                Method method,                  // 被调用方法的字节码对象
                Object[] args){                      //  方法的参数数组            

                              //参数可做修改   返回值可做修改  
                              // 增加事物操作
                              System.out.println(&quot;打开事物&quot;)
                              Objecct invoke =   method.invoke(target, args)  //反射调用方法
                               System.out.println(&quot;关闭事物&quot;)

          } 
             ) ;
</code></pre><h2 id="SpringAop依赖那种代理实现，有什么区别"><a href="#SpringAop依赖那种代理实现，有什么区别" class="headerlink" title="SpringAop依赖那种代理实现，有什么区别"></a>SpringAop依赖那种代理实现，有什么区别</h2><blockquote>
<p>动态代理    代理对象存在接口时 使用动态代理    依赖接口<br>Cglib          对目标对象进行继承实现类功能的加强</p>
</blockquote>
<h2 id="Spring的通知（Advice）有哪几种类型，分别有什么特点"><a href="#Spring的通知（Advice）有哪几种类型，分别有什么特点" class="headerlink" title="Spring的通知（Advice）有哪几种类型，分别有什么特点"></a>Spring的通知（Advice）有哪几种类型，分别有什么特点</h2><pre><code>    before               前置通知(方法执行前)  
    after-returning  后置通知 (方法正常返回)  
    around               环绕通知                                
    after-throwing    异常拦截通知  
    after                   后通知(一定会在方法结束前指定,不管是正常返回 还是抛出异常)  
             xml 配置环绕通知  通知类的写法
public Object around(ProceedingJoinPoint pjp)throws Throwable{
          System.out.println(&quot;这是环绕通知前的部分&quot;);
               Object proceed = pjp.proceed(); //调用目标方法
          System.out.println(&quot;环绕通知之后的部分&quot;)
           return proceed;
     }
</code></pre><p> 注解配置  </p>
<pre><code>1.  编写界面类        打上注解 @Aspect        如果不打算在xml中声明组件  还可以打上@Component  
2.  @Pointcut(&quot;execution(* com.ijavac.aop..*(..))&quot; ) 配置切面表达式   表示为哪些方法 , 哪些类 进行方法增强      
3.  编写通知方法  

<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"> @Before(&quot;aspect()&quot;)</span><br><span class="line">        public void before(JoinPoint joinPonit)&#123;</span><br><span class="line">            System.out.println(&quot;执行before...&quot;); </span><br><span class="line">        //after    afterthrowing  afterReturning同理  </span><br><span class="line">   &#125; </span><br><span class="line">        </span><br><span class="line">@Around(&quot;aspect()&quot;)</span><br><span class="line">          public void around(JointPoint joinPoint)&#123;</span><br><span class="line">            System.out.println(&quot;执行Around 前...&quot;);</span><br><span class="line">                       (ProceedingJoinPoint) joinPoint.proceed();</span><br><span class="line">             System.out.println(&quot;执行Around 后...&quot;);</span><br><span class="line">             </span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
</code></pre>]]></content>
      
        <categories>
            
            <category> Java </category>
            
        </categories>
        
        
        <tags>
            
            <tag> Java </tag>
            
            <tag> Spring </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[Spring 入门学习(一)]]></title>
      <url>//blog/2017/01/24/Spring-%E5%85%A5%E9%97%A8%E5%AD%A6%E4%B9%A0-%E4%B8%80/</url>
      <content type="html"><![CDATA[<h2 id="一-软件应用分层架构"><a href="#一-软件应用分层架构" class="headerlink" title="一,软件应用分层架构"></a>一,软件应用分层架构</h2><h3 id="标准三层架构"><a href="#标准三层架构" class="headerlink" title="标准三层架构:"></a>标准三层架构:</h3><p>1:数据访问层:主要是对原始数据(数据库或者文本文件等存放数据的形式)的操作层,而不是指原始数据,也就是说,是对数据的操作,而不是数据库,具体为业务逻辑层或表示层提供数据服务．<br>2:业务逻辑层:主要是针对具体的问题的操作,也可以理解成对数据层的操作,对数据业务逻辑处理,如果说数据层是积木,那逻辑层就是对这些积木的搭建。具体的讲主要负责对数据层的操作。也就是说把一些数据层的操作进行组合。<br>3:表示层:主要表示WEB方式,如果逻辑层相当强大和完善,无论表现层如何定义和更改,逻辑层都能完善地提供服务。主要对用户的请求接受,以及数据的返回,为客户端提供应用程序的访问。</p>
<p><img src="https://ww2.sinaimg.cn/mw1024/005MGUrjgw1f18bw0y8f2j30le0u0dis.jpg" alt="一拳超人"><br><a id="more"></a></p>
<p>优点:<br>    1:开发人员可以只关注整个结构中的其中某一层；<br>    2:可以很容易的用新的实现来替换原有层次的实现；<br>    3:可以降低层与层之间的依赖；<br>    4:有利于标准化；<br>    5:利于各层逻辑的复用。<br>    6:结构更加的明确<br>    7:在后期维护的时候,极大地降低了维护成本和维护时间</p>
<p>缺点:<br>    1:降低了系统的性能。这是不言而喻的。如果不采用分层式结构,很多业务可以直接造访数据库,以此获取相应的数据,如今却必须通过中间层来完成。<br>    2:有时会导致级联的修改。这种修改尤其体现在自上而下的方向。如果在表示层中需要增加一个功能,为保证其设计符合分层式结构,可能需要在相应的业务逻辑层和数据访问层中都增加相应的代码。<br>    3:增加了开发成本。</p>
<h2 id="二-Spring相关概念"><a href="#二-Spring相关概念" class="headerlink" title="二,Spring相关概念"></a>二,Spring相关概念</h2><blockquote>
<p>少数的jar+某一个功能的配置文件</p>
</blockquote>
<ol>
<li>轻量级的容器:<br>容器 : spring容器帮我们管理 业务逻辑层 ,里边有很多业务逻辑对象,有对象就有对象的生命周期的管理(创建,销毁).</li>
</ol>
<p>Spring容器 : 管理对象</p>
<p>轻量级:容器给予的业务逻辑对象多少种服务？spring给用户提供的服务完全有用户自己决定,spring想用什么服务自己开启使用。但是重量级的都是只要你用就把所有的服务都给你,不能自己定制。</p>
<p>spring容器从来不能独立运行,一定借助于其他容器启动,或<br>者借助web容器启动,或者ejb容器启动。</p>
<p>web容器 : tomcat</p>
<p>特点:<br> 应用模块之间耦合度小,组件(bean)都是可重用的,都是各自打包的, .jar</p>
<ol>
<li><p>why spring？</p>
<ol>
<li><p>动态解耦,方便开发,面向接口设计</p>
<p>IOC:控制反转<br>对象的创建销毁权利 –&gt; 反转—&gt;spring容器<br> 通过Spring提供的IoC容器,我们可以将对象之间的依赖关系交由Spring进行控制,避免硬编码所造成的过度程序耦合。有了Spring,用户不必再为单实例模式类、属性文件解析等这些很底层的需求编写代码,可以更专注于上层的应用</p>
</li>
<li><p>方便程序的测试TDD(Test-Driven Development)<br> 可以用非容器依赖的编程方式进行几乎所有的测试工作,在Spring里,测试不再是昂贵的操作,而是随手可做的事情。</p>
</li>
<li>降低Java EE API的使用难度<br> Spring对很多难用的Java EE API(如JDBC,JavaMail,远程调用等)提供了一个简单的封装层,通过Spring的简易封装,这些Java EE API的使用难度大为降低。</li>
<li>方便集成各种优秀框架<br> Spring不排斥各种优秀的开源框架,相反,Spring可以降低各种框架的使用难度,Spring提供了对各种优秀框架(如Struts,Hibernate、Hessian、Quartz)等的直接支持。<br> SSH : spring + struts + hibernate<br> SSM : spring + stringMVC + mybatis</li>
<li>AOP编程的支持<br> 通过Spring提供的AOP功能,方便进行面向切面的编程,许多不容易用传统OOP实现的功能可以通过AOP轻松应付。</li>
<li>声明式事务的支持<br> 在Spring中,我们可以从单调烦闷的事务管理代码中解脱出来,通过声明式方式灵活地进行事务的管理,提高开发效率和质量。<br>编程式事物:<br>estore: 程序员手动代码提交事物<pre><code>catch(){回滚事物}
</code></pre>事物的提交:<br>哪个？ –&gt; service–&gt;<br>一个业务逻辑完成以后 提交事物 <h2 id="service-做业务逻辑的处理"><a href="#service-做业务逻辑的处理" class="headerlink" title="   service:做业务逻辑的处理;"></a>   service:做业务逻辑的处理;</h2>++<br>提交事物(为了代码的完整性)<br>回滚事物<br>Spring声明式事物: 配置<br> 声明一下，在哪里提交事物 和 回滚事物<br> Spring</li>
<li>对异常的处理方式,所有的都转换成Unchecked的。</li>
<li>它不是一个一体化的解决方案。</li>
<li>良好的设计,容易扩展,很多可重用的组件</li>
</ol>
</li>
</ol>
<ol>
<li>Spring核心组件(我们主要学习的是IOC和AOP模块)<br> (1)、Spring Core(IOC) 核心容器,提供组件(bean)的创建、装配、销毁<br> (2)、Spring Context Spring上下文,是一个接口ApplicationContext(继承自BeanFactory接口)的实现<br> (3)、Spring Web容器,web应用上下文,是webApplicationContext接口的实现<br> (4)、Spring DAO容器,是SpringDAO 支持模块,是为了简化DAO的使用<br> (5)、Spring ORM<br> (6)、Spring AOP ,对AOP编程支持的模块<br> (7)、Spring MVC,类似于Spring表示层的一个框架</li>
</ol>
<ol>
<li><p>spring ioc(must):<br>IOC:Inversion of Control 控制反转 给spring容器(IOC容器)     </p>
<pre><code>   将创建对象的权利  反转给spring容器
一种说法:对象之间的依赖关系,由容器在运行时依据配置文件动态的建立
另一种说法:对象的控制器转移了,转到外部容器了,避免了代码的纠缠,代码更容易被维护,模板之间的耦合性降低,容易测试
</code></pre><p>IoC 控制反转意味着将你设计好的类交给容器去控制,而不是在类的内部进行控制,即控制权由应用代码中转到了外部容器<br>DI:Dependency Injection 依赖注入 ,组件不做定位查询,只提供相应方法,由容器创建对象,并调用相应方法设置所需对象需要的组件</p>
<p> IOC解决:对象谁来创建的问题<br>  DI解决:对象间的关系如何建立的问题。<br>   将容器中的 对象 注入到容器中的另一个对象中</p>
<pre><code>org.springframework.beans及org.springframework.context包是IOC容器的基础
</code></pre></li>
<li><p>SpringIOC核心api</p>
<p>BeanFactory接口和容器</p>
<p>BeanFactory是Spring中Bean容器,IoC的核心接口,主要用于处理Bean的初始化和配置,建立对象间的依赖关系</p>
<p>定义了如下方法:<br>Object getBean(String name) //根据指定名称返回一个Bean实例</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">&lt;T&gt; T getBean(Class&lt;T&gt; requiredType)   //返回一个与给定Class唯一匹配的Bean实例</span><br><span class="line"></span><br><span class="line">Class&lt;?&gt; getType(String name)       //得到名称为name的Bean的Class对象</span><br><span class="line"></span><br><span class="line">boolean isPrototype(String name)   //判断名称为name的Bean是否是原型,</span><br><span class="line">                                       //即是否总是返回一个新实例</span><br><span class="line">boolean isSingleton(String name)   //判断名称为name的Bean是否是单例</span><br><span class="line"></span><br><span class="line">boolean containsBean(String name)  //判断是否包含给定名称的Bean实例</span><br><span class="line"></span><br><span class="line">boolean isTypeMatch(String name, Class&lt;?&gt; targetType)</span><br><span class="line">                      //判断名称为name的Bean实例是否为targetType类型</span><br><span class="line">String[] getAliases(String name)  //如果名称为name的Bean有别名返回</span><br></pre></td></tr></table></figure>
<p>通过getBean方法便可以得到相应的类实例,但是最好永远不调用,而使用注入,<br>避免对Spring API的依赖</p>
<p>在Spring中,同一Spring容器中的bean默认情况下是Singleton(单例),将在bean<br>的作用域介绍.</p>
</li>
</ol>
<p>ApplicationContext接口<br>   该接口继承于BeanFactory,增强了BeanFactory,提供了事务处理AOP,国际化,事件传递</p>
<p>   所以在代码中我们一般会使用ApplicationContext接口,以及这个接口相应的实现类来创建spring的容器对象。<br>   例如:<br>   String path = “com/briup/ioc/set/set.xml”;<br>   ApplicationContext container = new ClassPathXmlApplicationContext(path);</p>
<p>ioc容器的创建:<br> 利用xml创建  abc.xml<br>  container = new ClassPathXMLAC(abc.xml)</p>
<ol>
<li><p>配置文件<br>Spring通过读取配置文件中的数据来对应用中各个对象进行实例化,配置以及组装,通常使用XML文件来作为配置文件。<br>XML基本结构如下:</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span><br><span class="line">&lt;beans xmlns=&quot;http://www.springframework.org/schema/beans&quot;</span><br><span class="line">    xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span><br><span class="line">    xmlns:context=&quot;http://www.springframework.org/schema/context&quot;</span><br><span class="line">    xsi:schemaLocation=&quot;http://www.springframework.org/schema/beans</span><br><span class="line">       http://www.springframework.org/schema/beans/spring-beans-3.2.xsd</span><br><span class="line">       http://www.springframework.org/schema/context</span><br><span class="line">       http://www.springframework.org/schema/context/spring-context-3.2.xsd&quot;&gt;</span><br><span class="line"></span><br><span class="line">&lt;/beans&gt;</span><br></pre></td></tr></table></figure>
<p>这个文件的头部声明可以在下载的spring文档中的示例找到.注意这里面用到了俩个schema文件(.xsd文件),就是spring-beans-3.2.xsd和spring-context-3.2.xsd,这俩个文件在下载的spring文档中也是可以找到的.我们在Eclipse中把xml文件和schema文件关联(注意上课的演示步骤)后,在这个xml中就可以有智能提示了。还有一点需要注意,将来我们这xml中再引入另外的schema文件后在配置的时候就可以智能提示出新的配置内容,spring是又很多模块组成的,我们需要什么功能的配置标签就需要引入对应的schema文件,从而获得智能提示,同时也能帮我们验证配置的正确性</p>
</li>
</ol>
<ol>
<li><p>IOC注入</p>
<ol>
<li>set方式注入(必须依靠set方法)    name=”xxx”  需要对应class中存在对应的 setXxx 方法<br>可以注入的内容有:<br>A、基本类型(8中基本类型+字符串)的装配<br>B、对象类型的装配<br>C、集合的装配</li>
</ol>
<p>A、基本类型的装配<br>   方式: 配置元素<value><br>   例子:</value></p>
   <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">public class HelloBean &#123;</span><br><span class="line">    private String name;</span><br><span class="line">    private int age;</span><br><span class="line">    public String sayHello()&#123;</span><br><span class="line">        return &quot;hello &quot;+name +&quot;,your age is&quot; + age;</span><br><span class="line">    &#125;</span><br><span class="line">    .............</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p> 配置文件set.xml</p>
 <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&lt;bean id=&quot;helloBean&quot; class=&quot;ioc.HelloBean&quot;&gt;</span><br><span class="line">    &lt;property name=&quot;name&quot;&gt;</span><br><span class="line">        &lt;value&gt;tom&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">        &lt;property name=&quot;age&quot; value=&quot;20&quot;&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">&lt;/bean&gt;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<pre><code>id是Bean的唯一标识,要求在整个配置文件中要唯一,也可使用name属性,bean标签里面的id和name属性都可以用来标识这个配置的对象,但是id会帮我们检查给对象起的名字是否规范(名字不能重复、不能用数字开头、不能有空格等等),如果检查出来了那么就会报错。name属性不会帮检查这些东西。
</code></pre><blockquote>
<p>property 对于所有用set方式来注入的必须改标签</p>
<p>value    是对以基本类型,都用value(标签/属性)来注入,可以实现自动的数据类型转换</p>
</blockquote>
<pre><code>测试类:
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">main:</span><br><span class="line"> ApplicationContext ac =</span><br><span class="line">     new ClassPathXmlApplicationContext(&quot;set.xml&quot;);    //可以指定多个配置文件</span><br><span class="line"> //获取容器的一个实例</span><br><span class="line"> HelloBean hb = (HelloBean) ac.getBean(&quot;helloBean&quot;);</span><br><span class="line"> System.out.println(hb.sayHello());</span><br></pre></td></tr></table></figure>
</code></pre><p>   B、对象类型的装配   </p>
<pre><code>  (1)、&lt;ref local=&quot; &quot;/&gt; 用于涉及的对象的id在当前配置文件中(用于在本配置文件中配置了的bean的引入同ref=&quot;..&quot;)
  (2)、&lt;ref bean=&quot; &quot;/&gt;  用于涉及的对象的id不在本配置文件中    //创建容器的时候 指定了多个配置文件 
(用于引用不在本配置文件中配置的bean)
  (3)、使用property的ref属性引用




public class OtherBean {
    private String str1;
    public String getStr1() {
        return str1;
    }
    public void setStr1(String str1) {
        this.str1 = str1;
    }
    public String toString(){
        return &quot;OtherBean &quot;+str1;
    }
}

public class SomeBean {
    private OtherBean ob;
    public void printInfo(){
        System.out.println(&quot;someBean &quot;+ob);
    }
    public OtherBean getOb() {
        return ob;
    }
    public void setOb(OtherBean ob) {
        this.ob = ob;
    }
}

配置applicationContext.xml
&lt;bean id=&quot;someBean&quot; class=&quot;ioc.SomeBean&quot;&gt;
    &lt;property name=&quot;ob&quot;&gt;
        &lt;ref bean=&quot;otherBean&quot; /&gt;
    &lt;/property&gt;
&lt;/bean&gt;
配置other.xml文件
&lt;bean id=&quot;otherBean&quot; class=&quot;ioc.OtherBean&quot;&gt;
    &lt;property name=&quot;str1&quot;&gt;
        &lt;value&gt;string1&lt;/value&gt;
    &lt;/property&gt;
&lt;/bean&gt;

测试类:
main:
    String[] path = {&quot;ioc/applicationContext.xml&quot;,&quot;ioc/other.xml&quot;};
    ApplicationContext ac = new ClassPathXmlApplicationContext(path);
    SomeBean sb = (SomeBean) ac.getBean(&quot;someBean&quot;);
    sb.printInfo();
</code></pre><p>   C、集合的装配<br>    方式:配置元素<list> <set> <map> <props></props></map></set></list></p>
<pre><code>public class SomeBean {
    private List listProperty;
    private Set setProperty;
    private Map mapProperty;
    private Properties&lt;String,String&gt; property;
    public List getListProperty() {
        return listProperty;
    }
    public void setListProperty(List listProperty) {
        this.listProperty = listProperty;
    }
    public Set getSetProperty() {
        return setProperty;
    }
    public void setSetProperty(Set setProperty) {
        this.setProperty = setProperty;
    }
    public Map getMapProperty() {
        return mapProperty;
    }
    public void setMapProperty(Map mapProperty) {
        this.mapProperty = mapProperty;
    }
    public Properties getProperty() {
        return property;
    }
    public void setProperty(Properties property) {
        this.property = property;
    }
    public void printInfo(){
        System.out.println(&quot;listProperty&quot;);
        System.out.println(listProperty);
        System.out.println(&quot;setProperty&quot;);
        System.out.println(setProperty);
        Set set = mapProperty.entrySet();
        Iterator it = set.iterator();
        while(it.hasNext()){
             Map.Entry entry = (Entry) it.next();
             System.out.println(&quot;Key &quot; +entry.getKey() );
             System.out.println(&quot;value &quot;+entry.getValue());
        }
        System.out.println(&quot;props: &quot;);
        Set set2 = property.entrySet();
        Iterator it2 = set2.iterator();
        while(it2.hasNext()){
            Map.Entry entry= (Entry) it2.next();
            System.out.println(&quot;key &quot;+entry.getKey());
            System.out.println(&quot;value &quot;+entry.getValue());
        }
    }
}

applcationContext.xml的写法:
&lt;bean id=&quot;someBean&quot; class=&quot;ioc.SomeBean&quot;&gt;
    &lt;property name=&quot;listProperty&quot;&gt;
         &lt;list&gt;
            &lt;value&gt;list1&lt;/value&gt;
            &lt;value&gt;list1&lt;/value&gt;
            &lt;value&gt;list3&lt;/value&gt;
         &lt;/list&gt;
    &lt;/property&gt;
    &lt;property name=&quot;setProperty&quot;&gt;
         &lt;set&gt;
            &lt;value&gt;set1&lt;/value&gt;
            &lt;value&gt;set1&lt;/value&gt;
            &lt;value&gt;set3&lt;/value&gt;
         &lt;/set&gt;
    &lt;/property&gt;
    &lt;property name=&quot;mapProperty&quot;&gt;
         &lt;map&gt;
            &lt;entry key=&quot;key1&quot;&gt;
              &lt;value&gt;value1&lt;/value&gt;
            &lt;/entry&gt;
            &lt;entry key=&quot;key2&quot;&gt;
              &lt;value&gt;value2&lt;/value&gt;
            &lt;/entry&gt;
         &lt;/map&gt;
    &lt;/property&gt;
    &lt;property name=&quot;property&quot;&gt;
         &lt;props&gt;
          &lt;prop key=&quot;key1&quot;&gt;prop1&lt;/prop&gt;
          &lt;prop key=&quot;key2&quot;&gt;prop2&lt;/prop&gt;
          &lt;prop key=&quot;key3&quot;&gt;prop3&lt;/prop&gt;
         &lt;/props&gt;
    &lt;/property&gt;
&lt;/bean&gt;

测试类:
main:
    String path = &quot;ioc/applicationContext.xml&quot;;
    ApplicationContext  ac =
        new ClassPathXmlApplicationContext(path);
    SomeBean sb = (SomeBean) ac.getBean(&quot;someBean&quot;);
    sb.printInfo();
</code></pre><ol>
<li><p>基于构造器注入<br>方式: 配置<constructor-arg>元素<br>在Bean中不用写set方法,但是要有相应的构造器</constructor-arg></p>
<p>构造器注入有俩种形式 一个是根据参数类型 一个是根据参数位置的下标</p>
</li>
</ol>
<pre><code>    &lt;constructor-arg type=&quot;int&quot; value=&quot;&quot;&gt;
    &lt;constructor-arg  index=&quot;0&quot; value=&quot;&quot;&gt;
例如:
&lt;bean name=&quot;student&quot; class=&quot;com.briup.bean.Student&quot;&gt;
        &lt;constructor-arg type=&quot;int&quot; value=&quot;25&quot;&gt;
        &lt;/constructor-arg&gt;

        &lt;constructor-arg type=&quot;java.lang.String&quot; value=&quot;tom&quot;&gt;
        &lt;/constructor-arg&gt;

        &lt;constructor-arg type=&quot;long&quot; value=&quot;100&quot;&gt;
        &lt;/constructor-arg&gt;

&lt;/bean&gt;

或者:
&lt;bean name=&quot;student&quot; class=&quot;com.briup.bean.Student&quot;&gt;
     &lt;constructor-arg index=&quot;2&quot;&gt;
        &lt;value&gt;30&lt;/value&gt;
     &lt;/constructor-arg&gt;

     &lt;constructor-arg index=&quot;0&quot;&gt;
        &lt;value&gt;200&lt;/value&gt;
     &lt;/constructor-arg&gt;

     &lt;constructor-arg index=&quot;1&quot;&gt;
        &lt;value&gt;lily&lt;/value&gt;
     &lt;/constructor-arg&gt;
&lt;/bean&gt;
</code></pre><ol>
<li><p>自动注入 :容器依照一些规则去装配bean中的一个属性                        装配引用型数据类型<br>   注意:自动装配只对[对象类型]起作用,对基本类型不起作用.<br>   第一种情况:<br>   在beans标签中配置装载方式:default-autowire=”byName”<br>   default-autowire=”byName”</p>
<pre><code>在根元素beans中加入这个属性,那么下面所有的bean都会
使用byName的方式进行自动注入,如果在下面的某一个bean
里面想使用其他的方式进行注入,可以用autowire=&quot;&quot;属性进行
说明,或者某一个bean不想使用任何自动注入就使用autowire=&quot;no&quot;
</code></pre><p>   第二种情况:<br>   在bean标签中指定配置方式<br>   autowire=”byName”:</p>
<pre><code>spring容器会到当前的类中找property的名字,然后
再根据这个名字去spring容器中找有没有和这个property
名字相同的对象,有的话,就把这个对象当做参数放到
setXxxx这个方法里面注入进来.
注意:了解property指的类中的什么东西。  property 是指类中 拥有 get set 方法 的 field
找到多个,报错
</code></pre><p>   autowire=”byType”:</p>
<pre><code>spring容器会根据当前类中的set方法里面参数的类型,
去容器中找相匹配的对象,如果没找到就算了,如果找到
一个就注入进来,如果找到多个,那么就会报错了.
</code></pre><p>   autoWrite=”constructor”</p>
<pre><code>根据构造器的参数类型去匹配
</code></pre></li>
</ol>
<ol>
<li><p>继承:并不是oo的继承关系<br>   bean的定义的继承,指bean的配置可去继承</p>
<pre><code>true 抽象化 代码中不能getBean获取其对象
</code></pre><p>   abstract =</p>
<pre><code>false 默认
</code></pre><p>   parent = “父类bean的id/name”</p>
<p>   例子:</p>
 <bean name="student" class="com.briup.bean.Student"><br>     <property name="name"><br>         <value>zhangsan</value><br>     </property><br> </bean>



</li>
</ol>
<pre><code>&lt;!--
    abstract=&quot;true&quot; 表示当前的配置是一个抽象的配置,
    这时候我们在代码中就不能通过这个bean的名字teacher来
    获得相应的对象了(和java中的抽象类不能直接new对象的道理一样)

    但是我们可以在写一个配置去继承这个抽象的配置,当然即使当前
    这个配置不是抽象的,也能够被继承(和java中继承一样)
--&gt;

&lt;bean name=&quot;teacher&quot; class=&quot;com.briup.bean.Teacher&quot; abstract=&quot;true&quot;&gt;
    &lt;property name=&quot;student&quot; ref=&quot;student&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;

&lt;!--
    parent=&quot;teacher&quot; 表示当前配置是继承了另外一个名字叫
    teacher的bean的配置,配置和配置的继承像java中的类和类
    直接的继承一样,子类会把父类中的对象继承过来.
    当然在子配置里面依然是可以覆盖父配置中已经写的配置信息.
--&gt;

&lt;bean name=&quot;t&quot; parent=&quot;teacher&quot;&gt;     //继承teacher  继承父配置中的东西

 &lt;property name=&quot;id&quot;&gt;
        &lt;value&gt;11&lt;/value&gt;
    &lt;/property&gt;

    &lt;property name=&quot;name&quot;&gt;
        &lt;value&gt;TeacherWang&lt;/value&gt;
    &lt;/property&gt;
&lt;/bean&gt;
</code></pre><ol>
<li><p>bean对象的生命周期</p>
<pre><code>单例管理的对象:
1. 默认情况下,spring在读取xml文件的时候,就会创建对象。
2. 在创建的对象的时候(先调用构造器),会去调用init-method=&quot;..&quot;
属性值中所指定的方法.
3. 对象在被销毁的时候,会调用destroy-method=&quot;...&quot;属性值中          spring容器关闭时被销毁
所指定的方法.(例如调用container.destroy()方法的时候)
4. lazy-init=&quot;true&quot;,可以让这个对象在第一次被访问的时候创建
</code></pre></li>
</ol>
<pre><code>非单例管理的对象:
1. spring读取xml文件的时候,不会创建对象.
2. 在每一次访问这个对象的时候,spring容器都会创建这个对象,并且   
调用init-method=&quot;..&quot;属性值中所指定的方法.
3. 对象销毁的时候,spring容器不会帮我们调用任何方法,
因为是非单例,这个类型的对象有很多个,spring容器一旦把
这个对象交给你之后,就不再管理这个对象了.
</code></pre>]]></content>
      
        <categories>
            
            <category> Java </category>
            
        </categories>
        
        
        <tags>
            
            <tag> Java </tag>
            
            <tag> Spring </tag>
            
        </tags>
        
    </entry>
    
  
  
</search>
